# Классификация знака ddG на основе данных SKEMPI 2.0

Этот проект реализует и сравнивает различные модели машинного обучения (включая нейронные сети и другие классификаторы) для предсказания знака изменения свободной энергии связывания (ΔΔG) при мутациях в белках, используя данные из базы SKEMPI 2.0. Основная цель - продемонстрировать процесс работы с данными, сравнить различные стратегии обработки дисбаланса классов и оценить производительность разных моделей.

## 📊 Исследовательский Анализ Данных (EDA)

Перед построением моделей был проведен исследовательский анализ данных (EDA) для понимания структуры датасета, распределений ключевых переменных, выявления взаимосвязей и потенциальных проблем, таких как пропущенные значения или несбалансированность классов.

### Визуализация результатов EDA

Основные результаты EDA сведены в единое изображение ниже:

![Результаты Исследовательского Анализа Данных](picture/eda_composite_figure.png)

**Описание графиков на изображении:**

*   **(A) ΔΔG по основным локациям мутаций:** Диаграмма размаха ("ящик с усами") и Stripplot, показывающие распределение значений ΔΔG для мутаций, расположенных в ядре (COR), на поверхности (SUR), по краю (RIM), внутри интерфейса (INT) и поддерживающих интерфейс (SUP). Видны различия в медианах и разбросе ΔΔG в зависимости от локации.
*   **(B) Зависимость ΔΔG от позиции мутации:** Диаграмма рассеяния (Scatter plot), показывающая отсутствие явной линейной или простой нелинейной зависимости между позицией аминокислотного остатка мутации и значением ΔΔG. Красная пунктирная линия отмечает ΔΔG = 0.
*   **(C) Корреляционная матрица числовых признаков:** Тепловая карта (Heatmap), показывающая коэффициенты корреляции Пирсона между числовыми параметрами. Заметны умеренные корреляции между ΔΔG и некоторыми кинетическими параметрами.
*   **(D) Распределение изменения энергии связывания (ΔΔG):** Гистограмма с ядерной оценкой плотности (KDE) для ΔΔG. Распределение близко к нормальному, немного смещено влево от нуля, указывая на преобладание стабилизирующих мутаций (ΔΔG < 0).
*   **(E) Распределение позиций мутаций:** Гистограмма показывает неравномерное распределение позиций мутаций вдоль цепи.
*   **(F, левый нижний) Распределение log<sub>10</sub>(K<sub>d</sub>):** Гистограммы логарифмов констант диссоциации для мутанта и дикого типа, показывающие широкий диапазон значений аффинности.
*   **(G) Частоты аминокислотных замен:** Парные столбчатые диаграммы, показывающие частоту встречаемости исходных (WT) и мутированных аминокислот.
*   **(K) Топ-10 локаций мутаций:** Горизонтальная столбчатая диаграмма наиболее частых аннотаций местоположения мутаций.
*   **(F, правый нижний) Распределение знака ΔΔG:** Столбчатая диаграмма количества стабилизирующих (Negative, ΔΔG < 0) и дестабилизирующих (Positive, ΔΔG > 0) мутаций. Классы **несбалансированы** (для подвыборки 'Other' соотношение ~1:3), стабилизирующие мутации преобладают.

### Основные выводы EDA

1.  **Данные:** Исходный датасет содержит ~7000 записей. После очистки ~6500 записей пригодны для анализа. Значительные пропуски в термодинамических и кинетических параметрах.
2.  **Целевая переменная (ΔΔG):** Распределение близко к нормальному. Задача предсказания знака ΔΔG является **несбалансированной**, класс 'Negative' (ΔΔG < 0) доминирует (~74% для 'Other').
3.  **Признаки:** Локация мутации, типы аминокислот и их физ.-хим. свойства важны. Позиция сама по себе связана с ΔΔG слабо.
4.  **Рекомендации для моделирования:** Использовать методы, устойчивые к дисбалансу, или техники его компенсации. Применять адекватные метрики (Balanced Accuracy, MCC, ROC AUC, PR AUC).

## ⚙️ Подход к Моделированию и Сравнению Стратегий

На основе выводов EDA был реализован пайплайн для сравнения различных моделей и стратегий обработки дисбаланса классов:

1.  **Предобработка:**
    *   Расчет ΔΔG и создание бинарной целевой переменной (`sign_ddG`).
    *   Извлечение признаков: тип WT/мутантной аминокислоты, позиция, локация.
    *   Инженерия признаков: расчет изменений физико-химических свойств аминокислот.
    *   Обработка пропусков (KNNImputer для температуры).
    *   Фильтрация данных по локализации ('Other').
2.  **Подготовка данных для моделей:**
    *   Разделение на обучающую и тестовую выборки (75/25) со стратификацией.
    *   **One-Hot Encoding** категориальных признаков.
    *   **Standard Scaling** числовых признаков.
    *   **Отбор признаков:** `SelectKBest` (выбрано 30 лучших).
3.  **Модели для Тестирования:**
    *   Логистическая регрессия (`LogisticRegression`)
    *   Многослойный перцептрон (`MLPClassifier`)
    *   Случайный лес (`RandomForestClassifier`)
    *   Экстремальные деревья (`ExtraTreesClassifier`)
    *   Градиентный бустинг (`GradientBoostingClassifier`)
    *   Адаптивный бустинг (`AdaBoostClassifier`)
    *   Метод опорных векторов (`SVC`)
    *   K-ближайших соседей (`KNeighborsClassifier`)
    *   Наивный Байес (`GaussianNB`)
    *   Стохастический градиентный спуск (`SGDClassifier`)
    *   CatBoost (`CatBoostClassifier`)
    *   LightGBM (`LGBMClassifier`)
    *   XGBoost (`XGBClassifier`)
4.  **Стратегии Обработки Дисбаланса:**
    *   Без обработки (`None`)
    *   Взвешивание классов (`Weights`, где применимо)
    *   Подбор порога (`Th-d`, `Th-d + W`)
    *   Oversampling (`RandomOverSampler`, `SMOTE`, `ADASYN`)
    *   Undersampling (`RandomUnderSampler`, `NearMiss-1`, `NearMiss-2`)
    *   Очистка / Гибриды (`TomekLinks`, `EditedNearestNeighbours`)
5.  **Оценка:** Расчет 8 метрик качества (`accuracy`, `balanced_accuracy`, `kappa`, `f1`, `mcc`, `logloss`, `roc_auc`, `pr_auc`) на тестовой выборке для каждой комбинации "модель + стратегия".

## 📈 Сравнение Моделей и Стратегий: Результаты

Результаты обширного сравнения моделей и стратегий представлены на изображении ниже, включающем тепловые карты для ключевых метрик (F1, MCC) и фасетные графики, иллюстрирующие производительность по всем 8 метрикам.

![Результаты Сравнения Моделей и Стратегий](picture/model_comparison_visuals.png)

**Подпись к изображению:** Визуальное сравнение производительности 10+ моделей классификации в сочетании с 12 стратегиями обработки дисбаланса классов. **(A)** Тепловые карты для метрик F1-score и Коэффициент Корреляции Мэтьюса (MCC) (более светлые/яркие ячейки соответствуют лучшим значениям в палитре `crest`). **(B)** Фасетный график типа "stripplot", показывающий распределение всех 8 метрик для каждой модели (ось X) и стратегии (цвет/маркер точки). **(C)** Фасетные графики типа "stripplot", сравнивающие стратегии (ось X) по 4 ключевым метрикам для отдельных моделей.

### Детальный Анализ Результатов (на основе Визуализаций)

Сравнительный анализ тепловых карт и фасетных графиков выявляет ключевые закономерности и компромиссы при работе с несбалансированными данными SKEMPI 2.0:

1.  **Разные Оптимумы для Метрик:** Стратегии, лидирующие по F1-мере, отличаются от лидеров по MCC.
    *   **F1-Score:** Наивысшие значения (>0.84) достигаются базовыми стратегиями (`None`, `Th-d`, `TLinks`) для сильных моделей (MLP, LR, SVC, RF, XGBoost). Это отражает хорошую производительность на **доминирующем классе '1' (ddG < 0)**.
    *   **MCC:** Наивысшие значения (>0.25) достигаются при использовании **Oversampling (`RandOS`, `SMOTE`, `ADASYN`)** или взвешивания классов (`Weights`, где применимо - LR, SVC, RF, LGBM). Это указывает на лучшую **сбалансированную производительность** по обоим классам. `MLPClassifier` с `RandOS` показал лучший MCC (0.263).

2.  **Эффективность Стратегий Обработки Дисбаланса:**
    *   **Oversampling (`RandOS`, `SMOTE`, `ADASYN`):** Наиболее надежный способ **повысить сбалансированные метрики (MCC, Balanced Accuracy, Kappa)** для большинства моделей, хотя часто немного снижает F1/Accuracy.
    *   **Baseline (`None`) / Очистка (`TLinks`):** Лучшие для **F1/Accuracy**, но дают посредственные MCC/Balanced Accuracy. `TLinks` иногда слегка улучшает AUC/LogLoss.
    *   **Взвешивание Классов (`Weights`):** Эффективно (повышает MCC/Balanced Accuracy) для моделей, поддерживающих `class_weight='balanced'`. Сравнимо с Oversampling.
    *   **Подбор Порога (`Th-d`):** Оптимизирует под F1/Accuracy, но **не улучшает баланс (MCC/Balanced Accuracy) или AUC**.
    *   **Undersampling (`RandUS`, `NM1`, `NM2`):** `RandUS` неэффективен. **`NearMiss` (`NM1`, `NM2`) приводит к катастрофическому падению качества** и абсолютно не рекомендуется.
    *   **Очистка (`ENN`):** Менее эффективна, чем `TLinks` и Oversampling/Weights.

3.  **Сравнение Моделей:**
    *   **Лидеры:** Ансамбли деревьев (**`RandomForest`, `CatBoost`, `LightGBM`, `XGBoost`, `GradientBoosting`**) и **`MLPClassifier`** показывают сильные результаты и хорошо адаптируются к стратегиям Oversampling/Weights для улучшения баланса.
    *   **Надежные Базовые:** `LogisticRegression` и `SVC` хорошо работают с `Weights` или Oversampling.
    *   **Менее Подходящие:** `KNeighbors` и `GaussianNB` показывают более низкую и менее стабильную производительность.

### Выводы и Рекомендации

*   Игнорирование дисбаланса (`None`) приводит к **смещенным моделям**.
*   Для **лучшей сбалансированной производительности (макс. MCC/Balanced Accuracy)**: используйте **Oversampling (`RandOS`/`SMOTE`)** или **взвешивание (`Weights`)** с моделями **RF, Boostings (LGBM/CatBoost/XGBoost) или MLP**.
*   Для **лучшей F1-меры/Accuracy (фокус на классе ddG < 0)**: используйте те же сильные модели с **`None`** или **`TomekLinks`**.
*   **Избегайте `NearMiss`**.
*   Эксперимент подтверждает, что **модели успешно обучаются** на данных SKEMPI 2.0, и выбор адекватной стратегии обработки дисбаланса **критически важен** для достижения желаемой производительности (особенно сбалансированной).

## 🚀 Дальнейшие Улучшения

Хотя сравнительный анализ дает ценную информацию, для дальнейшего улучшения качества предсказаний и понимания можно предпринять следующие шаги:

1.  **Инженерия Признаков:**
    *   **Структурные Признаки:** Включение 3D-информации (ΔSASA, глубина залегания, локальное окружение, водородные связи, интерфейсные признаки) является наиболее перспективным направлением.
    *   **Эволюционные Признаки:** Использование информации о консервативности позиций (например, из PSSM).
    *   **Расширенные Физ.-Хим. Свойства:** Добавление большего числа индексов из базы AAindex.
2.  **Улучшение Моделирования:**
    *   **Настройка Гиперпараметров:** Оптимизация параметров для лучших комбинаций "модель + стратегия" (например, с помощью Optuna, Hyperopt).
    *   **Ансамбли Моделей:** Объединение предсказаний лучших моделей (усреднение, стекинг).
    *   **Продвинутые Архитектуры:** Исследование Graph Neural Networks (GNN) при наличии структурных данных, или более глубоких MLP/1D-CNN.
    *   **Регрессионная Задача:** Предсказание не знака, а *значения* ΔΔG.
3.  **Оценка и Анализ:**
    *   **Надежная Кросс-Валидация:** Использование Stratified K-Fold или Leave-One-Protein/Complex-Out CV для финальной оценки.
    *   **Статистическая Значимость:** Сравнение лучших методов с помощью стат. тестов.
    *   **Анализ Ошибок:** Изучение случаев, где лучшие модели ошибаются.
    *   **Интерпретация Моделей:** Использование SHAP или LIME для определения важности признаков.
4.  **Улучшение Кода и Рабочего Процесса:**
    *   **Рефакторинг:** Вынесение функций в `.py` модули.
    *   **Пайплайны:** Использование `sklearn.pipeline.Pipeline` / `imblearn.pipeline.Pipeline`.
    *   **Конфигурация:** Вынесение параметров в YAML-файлы.
    *   **Логирование:** Использование библиотеки `logging`.

## 📁 Структура Директории

```
ddG_sign_classification/
│
├── scripts/
│   ├── ddG_sign_classification_neural_nets_fixed_v7.py  # Пример скрипта ML
│   ├── ddG_sign_classification_notebook.ipynb           # Основной Jupyter Notebook
│   └── comparison_results_*.html                        # HTML-таблицы сравнения (генерируются)
├── data/
│   └── skempi_v2.csv                                    # Датасет SKEMPI 2.0 (скачать отдельно!)
├── environment/
│   ├── requirements.txt                                 # Зависимости pip
│   └── environment.yml                                  # Окружение conda
├── picture/                                             # Изображения для README
│   ├── eda_composite_figure.png                         # Сводный график EDA
│   └── model_comparison_visuals.png                     # Сводный график сравнения моделей
├── eda_plots_*/                                         # Графики EDA (генерируются)
│   └── ... (*.png)
├── heatmap_*.png                                        # Тепловые карты сравнения (генерируются)
├── strategies_comparison_*.png                          # Графики сравнения стратегий (генерируются)
├── README.md                                            # Этот файл
└── LICENSE                                              # Лицензия проекта (MIT)

```
*(Структура директории обновлена)*

## 🛠️ Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd ddG_sign_classification
    ```

2.  **Скачайте датасет:**
    *   Загрузите файл `skempi_v2.csv` с официального ресурса базы данных SKEMPI 2.0.
    *   Поместите скачанный файл в директорию `data/`.

3.  **Настройте окружение:** Рекомендуется использовать `conda`.
    ```bash
    conda env create -f environment/environment.yml
    conda activate ddg_env
    # Установите недостающие библиотеки (если нужно)
    # pip install catboost lightgbm xgboost imbalanced-learn seaborn joblib ...
    ```
    Или используйте `pip`:
    ```bash
    python -m venv venv
    source venv/bin/activate # (или venv\Scripts\activate на Windows)
    pip install -r environment/requirements.txt
    # Установите CatBoost, LightGBM, XGBoost отдельно, если нужно
    ```

## ▶️ Использование

### Jupyter Notebook

Основной способ работы - через Jupyter Notebook `scripts/ddG_sign_classification_notebook.ipynb`.

1.  Активируйте созданное окружение.
2.  Запустите Jupyter Lab или Notebook: `jupyter lab` или `jupyter notebook`.
3.  Откройте ноутбук и выполняйте ячейки последовательно. Ноутбук содержит:
    *   Загрузку и предобработку данных.
    *   Исследовательский анализ данных (EDA) с визуализациями.
    *   Код для проведения сравнительного эксперимента моделей и стратегий.
    *   Визуализацию результатов сравнения.

### Консольный скрипт (Пример)

Скрипт `scripts/ddG_sign_classification_neural_nets_fixed_v7.py` может служить примером для обучения одной модели (MLP) с базовыми настройками. Для запуска других моделей или стратегий используйте код из Jupyter Notebook или адаптируйте скрипт.

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл [LICENSE](LICENSE) для подробностей.
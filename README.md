# Классификация знака ddG на основе данных SKEMPI 2.0

Этот проект реализует простую модель машинного обучения (с использованием нейронных сетей) для предсказания знака изменения свободной энергии связывания (ΔΔG) при мутациях в белках на основе данных из базы SKEMPI 2.0. Основная цель - продемонстрировать процесс работы с данными и обучения модели, а не достижение максимального качества предсказаний.

## 📊 Исследовательский Анализ Данных (EDA)

Перед построением модели был проведен исследовательский анализ данных (EDA) для понимания структуры датасета, распределений ключевых переменных, выявления взаимосвязей и потенциальных проблем, таких как пропущенные значения или несбалансированность классов.

### Визуализация результатов EDA

Основные результаты EDA сведены в единое изображение ниже:

![Exploratory Data Analysis Results](picture/eda_composite_figure.png)
*(Если изображение не отображается, убедитесь, что файл `eda_composite_figure.png` находится в папке `picture` репозитория)*

**Описание графиков на изображении:**

*   **(A) ΔΔG по основным локациям мутаций:** Boxplot и Stripplot показывают распределение значений ΔΔG для мутаций, расположенных в ядре (COR), на поверхности (SUR), по краю (RIM), внутри интерфейса (INT) и поддерживающих интерфейс (SUP). Видны различия в медианах и разбросе ΔΔG в зависимости от локации, особенно для COR.
*   **(B) Зависимость ΔΔG от позиции мутации:** Scatter plot показывает отсутствие явной линейной или простой нелинейной зависимости между позицией аминокислотного остатка, где произошла мутация, и значением ΔΔG. Красная пунктирная линия отмечает ΔΔG = 0.
*   **(C) Корреляционная матрица числовых признаков:** Heatmap показывает коэффициенты корреляции Пирсона между основными числовыми параметрами (константы аффинности, кинетические константы, температура, ΔΔG, позиция). Заметны умеренные корреляции между ΔΔG и некоторыми кинетическими параметрами (koff_wt, kon_mut).
*   **(D) Распределение изменения энергии связывания (ΔΔG):** Гистограмма с оценкой плотности вероятности (KDE) для ΔΔG. Распределение близко к нормальному, с центром немного левее нуля, что указывает на небольшое преобладание стабилизирующих мутаций (ΔΔG < 0) в датасете. Красная пунктирная линия отмечает ΔΔG = 0.
*   **(E) Распределение позиций мутаций:** Гистограмма показывает, что мутации происходят не равномерно по всей длине белковых цепей; есть определенные "горячие" регионы (в начале последовательности) и много позиций с единичными мутациями.
*   **(F, левый нижний) Распределение log<sub>10</sub>(K<sub>d</sub>):** Гистограммы логарифмов констант диссоциации для мутанта (Mutant) и дикого типа (Wild Type). Показывает широкий диапазон значений аффинности в датасете.
*   **(G) Частоты аминокислотных замен:** Парные гистограммы показывают, какие аминокислоты чаще всего встречаются в исходной позиции (WT) и на какие аминокислоты они чаще заменяются (Mutated). L, A, R, E - частые исходные аминокислоты; A, L, V, I - частые результаты мутаций.
*   **(K) Топ-10 локаций мутаций:** Горизонтальная гистограмма показывает наиболее частые аннотации местоположения мутаций, включая как основные категории (COR, RIM, SUP), так и их комбинации.
*   **(F, правый нижний) Распределение знака ΔΔG:** Гистограмма показывает количество мутаций со стабилизирующим эффектом (Negative, ΔΔG < 0) и дестабилизирующим эффектом (Positive, ΔΔG > 0). Классы относительно сбалансированы, но стабилизирующих мутаций немного больше.

### Основные выводы EDA

1.  **Данные:** Исходный датасет содержит ~7000 записей. После вычисления и очистки ΔΔG (удаление пропусков и нулевых значений) для анализа доступно около 6500-6800 записей. Значительное количество пропусков присутствует в термодинамических (ΔH, ΔS ~94%) и кинетических (kon, koff ~74%) параметрах, что ограничивает их прямое использование без специальной обработки.
2.  **Целевая переменная (ΔΔG):** Распределение ΔΔG близко к нормальному, слегка смещено в отрицательную сторону. Это отражается в относительном балансе классов для предсказания знака ΔΔG (Negative vs Positive), хотя класс "Negative" (ΔΔG < 0) немного преобладает.
3.  **Признаки:**
    *   **Позиция мутации:** Распределена неравномерно, сильной прямой связи с ΔΔG не наблюдается.
    *   **Локация мутации:** Категория локации (COR, RIM, SUR и т.д.) влияет на распределение ΔΔG, что делает её важным признаком. Категорию 'COR' (ядро) стоит рассматривать отдельно.
    *   **Типы аминокислот:** Частоты исходных и мутированных аминокислот различаются. Эта информация полезна для создания признаков на основе физико-химических свойств аминокислот (объем, гидрофобность, заряд и т.д.) и их изменений при мутации.
    *   **Числовые параметры:** Аффинности (Kd) и кинетические константы (kon/koff) распределены в широком диапазоне (лучше анализировать логарифмы). Есть умеренные корреляции между некоторыми из них и ΔΔG. Температура также показывает слабую корреляцию.
4.  **Рекомендации для моделирования:** Необходимо обработать пропуски (как минимум, в используемых признаках), закодировать категориальные признаки (локация, типы AA), нормализовать числовые признаки. Учитывая относительный баланс классов знака ΔΔG, стандартные метрики классификации применимы, но можно использовать техники для работы с небольшим дисбалансом (взвешивание классов, Focal Loss, over/undersampling). Физико-химические свойства аминокислот являются перспективными признаками для инженерии.

## ⚙️ Подход к Моделированию

На основе выводов EDA был реализован следующий пайплайн:

1.  **Предобработка:**
    *   Расчет ΔΔG и создание бинарной целевой переменной (`sign_ddG`).
    *   Извлечение признаков: тип WT/мутантной аминокислоты, позиция, локация.
    *   Инженерия признаков: расчет изменений физико-химических свойств аминокислот (объем, гидрофобность, заряд и т.д.) при мутации.
    *   Обработка пропусков в числовых признаках (например, KNNImputer для температуры, удаление строк с NaN в других ключевых признаках).
    *   Фильтрация данных по локализации мутации (`COR` или `Other`).
2.  **Подготовка данных для модели:**
    *   Разделение на обучающую и тестовую выборки.
    *   **Oversampling** миноритарного класса в обучающей выборке для улучшения баланса.
    *   **One-Hot Encoding** категориальных признаков (`WT_AA`, `MUT_AA`, `Location`).
    *   **Standard Scaling** числовых признаков.
    *   **Отбор признаков:** Использование `SelectKBest` для выбора 30 наиболее информативных признаков.
3.  **Обучение и оценка моделей:**
    *   **MLPClassifier (Scikit-learn):** Многослойный перцептрон с подбором гиперпараметров через `GridSearchCV` и взвешиванием классов (`class_weight='balanced'`).
    *   **PyTorch Neural Network (BatchNorm):** Нейронная сеть с батч-нормализацией, функцией потерь `BCELoss` (с `pos_weight`), оптимизатором Adam и ранней остановкой.
    *   **PyTorch Neural Network (Focal Loss):** Аналогичная сеть, но с использованием `FocalLoss` для явного акцента на сложных примерах при дисбалансе.
    *   **Метрики:** Accuracy, F1-score, Matthews Correlation Coefficient (MCC), ROC AUC. Сравнение с базовой стратегией (предсказание наиболее частого класса).

## 📈 Результаты (Пример)

Модели были успешно обучены, демонстрируя способность к обучению на данных (метрики на тестовой выборке превышали базовый уровень). Пример лучших метрик, полученных для одной из моделей (например, PyTorch с Focal Loss) на тестовой выборке для локаций 'Other':

*   Accuracy: ~0.67
*   F1 Score: ~0.74
*   MCC: ~0.31
*   ROC AUC: ~0.71

*(Примечание: Эти значения являются примерными и могут незначительно варьироваться при каждом запуске. Оптимизация метрик не была главной целью.)*

## 📁 Структура Директории

```
ddG_sign_classification/
│
├── scripts/
│   ├── ddG_sign_classification_neural_nets_fixed_v7.py  # Основной скрипт для запуска из консоли
│   └── ddG_sign_classification_notebook.ipynb           # Версия в формате Jupyter Notebook для интерактивного анализа
├── data/
│   └── skempi_v2.csv                                   # Датасет SKEMPI 2.0 (необходимо скачать отдельно!)
├── environment/
│   ├── requirements.txt                                # Файл зависимостей для pip
│   └── environment.yml                                 # Файл зависимостей для Conda
├── eda_plots_unified_theme_png/                        # Папка с графиками EDA (пример)
│   └── ... (файлы .png)
├── picture/                                            # Папка с изображениями для README
│   └── eda_composite_figure.png                        # Составное изображение EDA
├── README.md                                           # Этот файл
└── LICENSE                                             # Лицензия проекта (MIT)
```
*(Структура директории обновлена для отражения папки 'picture')*

## 🛠️ Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repo-url>
    cd ddG_sign_classification
    ```

2.  **Скачайте датасет:**
    *   Загрузите файл `skempi_v2.csv` с официального ресурса базы данных SKEMPI 2.0.
    *   Поместите скачанный файл в директорию `data/`. **Внимание:** Датасет не включен в репозиторий.

3.  **Настройте окружение (выберите один из способов):**

    *   **Используя Conda:**
        ```bash
        conda env create -f environment/environment.yml
        conda activate ddg_env
        ```

    *   **Используя pip и venv:**
        ```bash
        python -m venv venv
        # Активация окружения:
        # Linux/macOS:
        source venv/bin/activate
        # Windows:
        # venv\Scripts\activate
        pip install -r environment/requirements.txt
        ```

## 🚀 Использование

### Консольный скрипт

Основной скрипт `ddG_sign_classification_neural_nets_fixed_v7.py` можно запустить из командной строки.

*   **Запуск с настройками по умолчанию (данные из `data/skempi_v2.csv`, локация 'Other'):**
    ```bash
    python scripts/ddG_sign_classification_neural_nets_fixed_v7.py
    ```

*   **Запуск с указанием локации 'COR':**
    ```bash
    python scripts/ddG_sign_classification_neural_nets_fixed_v7.py --location COR
    ```

*   **Запуск с указанием другого пути к файлу данных:**
    ```bash
    python scripts/ddG_sign_classification_neural_nets_fixed_v7.py --data_path /path/to/your/skempi_v2.csv
    ```

Скрипт выполнит все шаги: загрузку данных, предобработку, обучение моделей, оценку и вывод результатов в консоль. Также будет сохранен файл с матрицей ошибок `confusion_matrix_<location>.png` для лучшей модели.

### Jupyter Notebook

Для интерактивного анализа и визуализации используйте `ddG_sign_classification_notebook.ipynb`.

1.  Активируйте созданное окружение (`conda activate ddg_env` или `source venv/bin/activate`).
2.  Запустите Jupyter Notebook или Jupyter Lab:
    ```bash
    jupyter notebook
    # или
    jupyter lab
    ```
3.  В интерфейсе Jupyter перейдите в директорию `scripts/` и откройте файл `ddG_sign_classification_notebook.ipynb`.
4.  Выполняйте ячейки кода последовательно.

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT - см. файл [LICENSE](LICENSE) для подробностей.
```